<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Solid background color (optional) -->
    <solid android:color="@android:color/transparent" />

    <!-- Black border -->
    <stroke
        android:width="4dp"
        android:color="@android:color/black" />

    <!-- Internal padding -->


</shape>